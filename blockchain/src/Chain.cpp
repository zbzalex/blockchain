#include <blockchain/Chain.hpp>
#include <blockchain/Tx.hpp>
#include <string.h>
#include <cstring>
#include <blockchain/Block.hpp>

////////////////////////////////////////////////////////////////////////////////
//
////////////////////////////////////////////////////////////////////////////////
Chain::Chain(){

};

////////////////////////////////////////////////////////////////////////////////
//
////////////////////////////////////////////////////////////////////////////////
Chain::~Chain()
{
  m_transactions.clear();
  m_blocks.clear();
};

////////////////////////////////////////////////////////////////////////////////
//
////////////////////////////////////////////////////////////////////////////////
bool Chain::addToPending(Tx *tx)
{
  int size = m_transactions.size();
  tx->setIndex(size + 1);

  decltype(m_transactions)::iterator it = m_transactions.begin();
  for (; it != m_transactions.end(); ++it)
  {
    if (strcmp(tx->getHash(), (*it)->getHash()) == 0)
    {
      return false;
    }
  }

  m_transactions.push_back(tx);

  return true;
};

////////////////////////////////////////////////////////////////////////////////
//
////////////////////////////////////////////////////////////////////////////////
std::vector<Tx *> Chain::getPendingTransactions()
{
  return m_transactions;
};

////////////////////////////////////////////////////////////////////////////////
// Add block to blockchain
// @return void
////////////////////////////////////////////////////////////////////////////////
void Chain::addBlock(Block *block)
{
  int size = m_blocks.size();

  block->setIndex(size + 1);

  m_blocks.push_back(block);

  block->setTransactions(m_transactions);

  m_transactions.clear();
};

////////////////////////////////////////////////////////////////////////////////
//
////////////////////////////////////////////////////////////////////////////////
std::vector<Block *> Chain::getBlocks()
{
  return m_blocks;
};

////////////////////////////////////////////////////////////////////////////////
//
////////////////////////////////////////////////////////////////////////////////
Block *Chain::getLastBlock()
{
  return *(m_blocks.end() - 1);
};

////////////////////////////////////////////////////////////////////////////////
//
////////////////////////////////////////////////////////////////////////////////
Block *Chain::newBlock()
{
  return new Block(getLastBlock()->getHash());
}